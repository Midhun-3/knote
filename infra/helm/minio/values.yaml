replicaCount: 1

image:
  repository: minio/minio
  tag: RELEASE.2022-10-29T06-21-33Z
  pullPolicy: IfNotPresent

accessKey: mykey
secretKey: mysecret

persistence:
  enabled: true
  size: 256Mi
  storageClass: ""

service:
  type: ClusterIP
  port: 9000

ingress:
  enabled: true
  className: nginx
  annotations: {}
  host: minio.local
  path: /minio
